{
  "name": "aws-ssm-parameter-sync-action",
  "main": "dist/index.js",
  "type": "module",
  "private": "true",
  "keywords": [
    "aws",
    "ssm"
  ],
  "author": "ankurk91",
  "license": "MIT",
  "description": "GitHub action to sync parameters to AWS SSM",
  "homepage": "https://github.com/ankurk91/aws-ssm-sync-action",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env ./src/index.js",
    "build": "ncc build ./src/index.js -o dist/"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@aws-sdk/client-ssm": "^3.901.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  },
  "engineStrict": true,
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=24.7.0"
  }
}
